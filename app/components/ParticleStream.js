import { h, Component } from "preact";
import { Entity, Scene } from "aframe-react";

export default class ParticleStream extends Component {

   
    
    render() {
        return (
            <div></div>
        )
    }
}
